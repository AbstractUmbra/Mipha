INSERT INTO botw_upgrade_path
VALUES (
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'soldier''s helm'
                 AND upgradeable = true ), 1,
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'chuchu jelly' ), 5,
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'bokoblin guts'), 3,
                                                 null,
                                                 null);


INSERT INTO botw_upgrade_path
VALUES (
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'soldier''s helm'
                 AND upgradeable = true ), 2,
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'keese wing' ), 5,
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'moblin guts'), 3,
                                               null,
                                               null);


INSERT INTO botw_upgrade_path
VALUES (
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'soldier''s helm'
                 AND upgradeable = true ), 3,
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'lizalfos tail' ), 2,
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'hinox guts'), 2,
                                              null,
                                              null);


INSERT INTO botw_upgrade_path
VALUES (
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'soldier''s helm'
                 AND upgradeable = true ), 4,
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'lynel hoof' ), 2,
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'lynel guts'), 2,
                                              null,
                                              null);


INSERT INTO botw_upgrade_path
VALUES (
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'soldier''s greaves'
                 AND upgradeable = true ), 1,
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'chuchu jelly' ), 5,
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'bokoblin guts'), 3,
                                                 null,
                                                 null);


INSERT INTO botw_upgrade_path
VALUES (
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'soldier''s armor'
                 AND upgradeable = true ), 2,
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'keese wing' ), 5,
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'moblin guts'), 3,
                                               null,
                                               null);


INSERT INTO botw_upgrade_path
VALUES (
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'soldier''s armor'
                 AND upgradeable = true ), 3,
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'lizalfos tail' ), 3,
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'hinox guts'), 1,
                                              null,
                                              null);


INSERT INTO botw_upgrade_path
VALUES (
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'soldier''s helm'
                 AND upgradeable = true ), 4,
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'lynel hoof' ), 2,
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'lynel guts'), 2,
                                              null,
                                              null);


INSERT INTO botw_upgrade_path
VALUES (
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'soldier''s greaves'
                 AND upgradeable = true ), 1,
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'chuchu jelly' ), 5,
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'bokoblin guts'), 3,
                                                 null,
                                                 null);


INSERT INTO botw_upgrade_path
VALUES (
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'soldier''s greaves'
                 AND upgradeable = true ), 2,
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'keese wing' ), 5,
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'moblin guts'), 3,
                                               null,
                                               null);


INSERT INTO botw_upgrade_path
VALUES (
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'soldier''s greaves'
                 AND upgradeable = true ), 3,
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'lizalfos tail' ), 3,
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'hinox guts'), 1,
                                              null,
                                              null);


INSERT INTO botw_upgrade_path
VALUES (
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'soldier''s greaves'
                 AND upgradeable = true ), 4,
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'lynel hoof' ), 2,
            (SELECT item_id
             FROM botw_items
             WHERE item_name = 'lynel guts'), 2,
                                              null,
                                              null);
\
